# Moved — this copy is retired

The StormTracker Windy.com plugin lives in its own repository:

**https://github.com/CAPFlyingFun/StormTracker-Windy-Plugin**

That repo is the single source of truth — it has the version history, issue
tracker, and active development (v1.4.0+ includes the GOES GLM lightning
layer). The copy that used to sit in this folder was a months-stale snapshot
that had already diverged; keeping two copies is how a Replit checkout ended
up pointed at the wrong repo. Nothing in this repository depends on the
plugin.
